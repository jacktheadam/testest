[package]
name = "aero-devices-storage"
version = "0.1.0"
rust-version.workspace = true
edition = "2021"
publish = false

[dependencies]
aero-storage = { path = "../aero-storage" }
aero-io-snapshot = { path = "../aero-io-snapshot" }
aero-devices = { path = "../devices" }
aero-platform = { path = "../platform" }
memory = { path = "../memory" }
