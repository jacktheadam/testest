[package]
name = "aero-net-pump"
version = "0.1.0"
rust-version.workspace = true
edition = "2021"
publish = false

[features]
default = ["virtio-net"]
virtio-net = ["dep:aero-virtio"]

[dependencies]
aero-net-backend = { path = "../aero-net-backend" }
aero-net-e1000 = { path = "../aero-net-e1000" }
aero-virtio = { path = "../aero-virtio", default-features = false, optional = true }
memory = { path = "../memory" }

[dev-dependencies]
aero-net-stack = { path = "../aero-net-stack" }
