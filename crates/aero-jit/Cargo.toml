[package]
name = "aero-jit"
version = "0.1.0"
rust-version.workspace = true
edition = "2021"
publish = false

[features]
default = []
legacy-baseline = ["aero-jit-x86/legacy-baseline"]
tier1-inline-tlb = ["aero-jit-x86/tier1-inline-tlb"]

[dependencies]
aero-jit-x86 = { path = "../aero-jit-x86" }
workspace-hack = { version = "0.1", path = "../workspace-hack" }
