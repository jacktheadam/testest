[package]
name = "aero-gpu-software"
version = "0.1.0"
rust-version.workspace = true
edition = "2021"
publish = false

[dependencies]
aero-devices-gpu = { path = "../aero-devices-gpu" }
aero-protocol = { path = "../aero-protocol" }
memory = { path = "../memory" }
workspace-hack = { version = "0.1", path = "../workspace-hack" }
