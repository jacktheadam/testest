{
  "name": "aero-net-proxy-server",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "WebSocket multiplexed TCP proxy relay for Aero (browser Windows emulator)",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=24.0.0 <25"
  },
  "scripts": {
    "check:node": "node ../../scripts/check-node-version.mjs",
    "prestart": "pnpm run check:node",
    "start": "node src/cli.js",
    "pretest": "pnpm run check:node",
    "test": "node --test"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
