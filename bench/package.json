{
  "name": "aero-bench",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0 <25"
  },
  "scripts": {
    "check:node": "node ../scripts/check-node-version.mjs",
    "pretest": "pnpm run check:node",
    "test": "node --test --experimental-strip-types --import ../scripts/register-ts-strip-loader.mjs history.test.js tests/*.test.ts"
  }
}
