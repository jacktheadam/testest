[package]
name = "aero-pci-firmware-adapter"
version = "0.1.0"
rust-version.workspace = true
edition = "2021"
publish = false

[dependencies]
aero-devices = { path = "../devices" }
firmware = { path = "../firmware" }

[dev-dependencies]
aero-cpu-core = { path = "../aero-cpu-core" }
aero-pci-routing = { path = "../aero-pci-routing" }
memory = { path = "../memory" }
