[package]
name = "aero-devices-input"
version = "0.1.0"
rust-version.workspace = true
edition = "2021"
publish = false

[dependencies]
aero-io-snapshot = { path = "../aero-io-snapshot" }

[dev-dependencies]
serde_json = "1"
