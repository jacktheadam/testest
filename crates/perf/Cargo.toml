[package]
name = "perf"
version = "0.1.0"
rust-version.workspace = true
edition = "2021"
publish = false

[dependencies]
aero-perf = { path = "../aero-perf" }
workspace-hack = { version = "0.1", path = "../workspace-hack" }
