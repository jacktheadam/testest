[package]
name = "firmware"
version = "0.1.0"
rust-version.workspace = true
edition = "2021"
publish = false

[dependencies]
aero-acpi = { path = "../aero-acpi" }
aero-edid = { path = "../aero-edid" }
aero-cpu-core = { path = "../aero-cpu-core" }
aero-pc-constants = { path = "../aero-pc-constants" }
aero-pci-routing = { path = "../aero-pci-routing" }
font8x8 = "0.3"
memory = { path = "../memory" }

[dev-dependencies]
aero-devices = { path = "../devices" }
aero-platform = { path = "../platform" }
