[package]
name = "win-offline-cert-injector"
version = "0.1.0"
rust-version.workspace = true
edition = "2021"
publish = false

[dependencies]
workspace-hack = { version = "0.1", path = "../../crates/workspace-hack" }
