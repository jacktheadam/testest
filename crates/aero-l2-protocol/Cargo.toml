[package]
name = "aero-l2-protocol"
version = "0.1.0"
rust-version.workspace = true
edition = "2021"
publish = false

[dependencies]

[dev-dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
